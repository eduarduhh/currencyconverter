spring:
  application: name=teste-jaya
  datasource:
    url: jdbc:h2:mem:currencydb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
api:
  url:
  base: EUR
  key: ef9bbd347f75a9eda6dd95df878d9732

server:
  error:
    include-message: always
    include-stacktrace: on_param
  port: 8080